using UnityEngine;

/// <summary>
/// Đánh dấu prefab/variant là boss (tuỳ chọn, cho designer).
/// Đếm presence nhạc nền thực tế dựa trên <see cref="EnemyScript.enemyType"/> sau spawn và khi chết (xem <see cref="DungeonOSTManager"/>).
/// </summary>
public class DungeonBossOstAuthority : MonoBehaviour
{
}
